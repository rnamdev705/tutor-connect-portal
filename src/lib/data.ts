/** @deprecated Import from `@/lib/types/domain` instead. Kept for gradual migration. */
export type {
  CaseDetail,
  CaseDocument,
  CaseItem,
  CaseStatus,
  DashboardStat,
  InvitedTutor,
  Tutor,
} from "@/lib/types/domain";
